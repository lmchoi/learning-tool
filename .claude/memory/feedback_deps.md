---
name: Add dependencies on a need-to basis
description: User prefers adding deps only when actually needed, not upfront
type: feedback
---

Do not front-load dependencies. Add runtime dependencies only when writing the code that requires them. Dev tooling (ruff, mypy, pytest, etc.) is fine to include upfront.
